---
title: Directory & DirectoryInfo
group: SHARD.FILESYSTEM
groupOrder: 7
order: 2
slug: directory-and-directoryinfo
type: Reference
---

<H2>Summary</H2>

<Prose>
  The <InlineCode>filesystem</InlineCode> namespace provides two directory abstractions:
  <InlineCode>Directory</InlineCode>, a static utility class for existence checks and
  creation by path, and <InlineCode>DirectoryInfo</InlineCode>, an instance-based wrapper
  around a directory path that supports path composition and stateful existence checks.
</Prose>

<H2>Syntax</H2>

<Prose>
  All members live in the <InlineCode>filesystem</InlineCode> namespace. Add{' '}
  <InlineCode>using filesystem;</InlineCode> at the top of a ShardScript file.
</Prose>

<CodeBlock
  code={`using stdio;
using filesystem;

namespace demo;

public static func Main() -> void
{
    // Static Directory API.
    exists: bool = Directory.Exists("D:/data");
    info: DirectoryInfo = Directory.Create("D:/data/temp");

    // DirectoryInfo instance API.
    println(info.FullName);
    println(info.Exists);

    // Path composition.
    sub: DirectoryInfo = info / "subfolder";
    file: FileInfo = info / "log.txt";
}`}
  language="csharp"
  filename="directory_overview.shard"
/>

<H2>Parameters / Arguments</H2>

<Prose>The static <InlineCode>Directory</InlineCode> class accepts the following inputs:</Prose>

<DocsTable
  headers={['Method', 'Parameter', 'Type', 'Description']}
  rows={[
    [<code>Exists(path)</code>, 'path', 'string', 'The directory path to test.'],
    [<code>Create(path)</code>, 'path', 'string', 'The directory path to create. Missing parents are created automatically.'],
    [<code>Delete(path)</code>, 'path', 'string', 'The directory path to delete. The directory must be empty.'],
  ]}
/>

<Prose>The <InlineCode>DirectoryInfo</InlineCode> constructor and members accept the following inputs:</Prose>

<DocsTable
  headers={['Member', 'Parameter', 'Type', 'Description']}
  rows={[
    [<code>new DirectoryInfo(fullPath)</code>, 'fullPath', 'string', 'The absolute or relative directory path to wrap.'],
    [<code>DirectoryInfo / segment</code>, 'segment', 'string', 'A relative directory or file name to append.'],
    [<code>Create()</code>, '—', '—', 'Creates the directory on disk, including any missing parents.'],
    [<code>Delete()</code>, '—', '—', 'Deletes the directory and all of its contents recursively.'],
  ]}
/>

<Callout tone="amber" title="Planned but not implemented">
  <InlineCode>Directory.GetFiles(path)</InlineCode>,{' '}
  <InlineCode>Directory.GetDirectories(path)</InlineCode>, and{' '}
  <InlineCode>Directory.GetCurrentDirectory()</InlineCode> are planned for a future release.
  They do not currently exist in the <InlineCode>shard.io</InlineCode> native implementation.
  To enumerate content today, store known names in a <InlineCode>string[]</InlineCode> and
  join them with the directory path manually.
</Callout>

<H2>Returns</H2>

<DocsTable
  headers={['Member', 'Return Type', 'Description']}
  rows={[
    [<code>Directory.Exists(path)</code>, 'bool', 'True if the path exists; false otherwise.'],
    [<code>Directory.Create(path)</code>, 'DirectoryInfo', 'A DirectoryInfo wrapping the newly created or existing directory.'],
    [<code>Directory.Delete(path)</code>, 'void', 'No return value.'],
    [<code>DirectoryInfo.FullName</code>, 'string', 'The full path used to construct the instance.'],
    [<code>DirectoryInfo.Name</code>, 'string', 'The last path component (the directory name).'],
    [<code>DirectoryInfo.Exists</code>, 'bool', 'True if the path refers to an existing directory.'],
    [<code>DirectoryInfo.Create()</code>, 'void', 'No return value.'],
    [<code>DirectoryInfo.Delete()</code>, 'void', 'No return value.'],
    [<code>DirectoryInfo / string</code>, 'DirectoryInfo or FileInfo', 'A new instance with the joined path.'],
  ]}
/>

<H2>Exceptions / Errors</H2>

<ul className="space-y-2 text-text-secondary">
  <Bullet>
    <strong className="text-text-primary">Create failure</strong> —{' '}
    <InlineCode>Directory.Create</InlineCode> and{' '}
    <InlineCode>DirectoryInfo.Create</InlineCode> throw a runtime exception if the directory
    tree cannot be created (for example, because a file with the same name already exists or
    because the caller lacks permission).
  </Bullet>
  <Bullet>
    <strong className="text-text-primary">Delete failure</strong> —{' '}
    <InlineCode>Directory.Delete(path)</InlineCode> throws if the directory does not exist or
    is not empty. <InlineCode>DirectoryInfo.Delete()</InlineCode> throws if the recursive
    removal fails.
  </Bullet>
  <Bullet>
    <strong className="text-text-primary">Path does not exist</strong> —{' '}
    <InlineCode>DirectoryInfo.Exists</InlineCode> simply returns false; it never throws for a
    missing path.
  </Bullet>
  <Bullet>
    <strong className="text-text-primary">Invalid path composition</strong> — The{' '}
    <InlineCode>/</InlineCode> operator creates a new path object without checking the disk.
    Errors surface only when the resulting path is used with a method that touches the file
    system.
  </Bullet>
</ul>

<H2>Remarks</H2>

<Prose>
  <strong>Static versus instance design.</strong> <InlineCode>Directory</InlineCode> is a
  static class (<InlineCode>LINK_STATIC</InlineCode>). It is never instantiated and every
  method resolves at compile time to <InlineCode>CALLSTATICMETHODSYMBOL</InlineCode>.{' '}
  <InlineCode>DirectoryInfo</InlineCode> is an instance class: it stores the wrapped path in
  the <InlineCode>FullName</InlineCode> backing field and allocates a new object for every
  path composition.
</Prose>

<Prose>
  <strong>Native backing.</strong> The implementation in{' '}
  <InlineCode>system/filesystem.shard.cpp</InlineCode> delegates to the C++ standard library's
  <InlineCode>std::filesystem</InlineCode> facilities. <InlineCode>Exists</InlineCode> calls{' '}
  <InlineCode>fs::exists</InlineCode>, <InlineCode>DirectoryInfo.Exists</InlineCode> calls{' '}
  <InlineCode>fs::is_directory</InlineCode>, and creation uses{' '}
  <InlineCode>fs::create_directories</InlineCode> (recursive mkdir-p semantics).
</Prose>

<Prose>
  <strong>Path composition.</strong> <InlineCode>DirectoryInfo / string</InlineCode> is
  overloaded. When the target type is <InlineCode>DirectoryInfo</InlineCode>, the segment is
  appended as a subdirectory. When the target type is <InlineCode>FileInfo</InlineCode>, the
  segment is treated as a file name and a <InlineCode>FileInfo</InlineCode> is produced. The
  operator never touches the disk; it only manipulates the path string.
</Prose>

<Prose>
  <strong>Delete semantics differ.</strong> <InlineCode>Directory.Delete(path)</InlineCode>
  requires the directory to be empty and uses <InlineCode>fs::remove</InlineCode>.{' '}
  <InlineCode>DirectoryInfo.Delete()</InlineCode> removes the directory tree recursively via{' '}
  <InlineCode>fs::remove_all</InlineCode>. Choose the static method for a safe, empty-only
  removal and the instance method when you intend to wipe nested contents.
</Prose>

<Callout tone="blue">
  The native callback behind <InlineCode>Directory.Create</InlineCode> is named
  <InlineCode>shard_directory_CreateDirectory</InlineCode>, but the public ShardScript member
  is <InlineCode>Create</InlineCode>. Use <InlineCode>Directory.Create(path)</InlineCode>, not
  <InlineCode>Directory.CreateDirectory(path)</InlineCode>.
</Callout>

<H2>Examples</H2>

<Prose>
  <strong>Creating and inspecting a directory.</strong>
</Prose>

<CodeBlock
  code={`using stdio;
using filesystem;

namespace demo;

public static func Main() -> void
{
    workDir: string = "D:/temp/shard_dir_demo";

    // Ensure a clean starting state.
    if (Directory.Exists(workDir))
    {
        Directory.Delete(workDir);
    }

    // Create the directory and inspect the returned DirectoryInfo.
    info: DirectoryInfo = Directory.Create(workDir);

    println(info.FullName);   // D:/temp/shard_dir_demo
    println(info.Name);       // shard_dir_demo
    println(info.Exists);     // true

    // Clean up.
    info.Delete();
    println(info.Exists);     // false
}`}
  language="csharp"
  filename="directory_create_inspect.shard"
/>

<Prose>
  <strong>Path composition with DirectoryInfo.</strong>
</Prose>

<CodeBlock
  code={`using stdio;
using filesystem;

namespace demo;

public static func Main() -> void
{
    root: DirectoryInfo = new DirectoryInfo("D:/app");

    // Compose a subdirectory path without touching the disk.
    configDir: DirectoryInfo = root / "config";
    println(configDir.FullName);   // D:/app/config

    // Compose a file path under the directory.
    settings: FileInfo = root / "config" / "settings.json";
    println(settings.FullName);    // D:/app/config/settings.json

    // Create the directory tree and write a file.
    configDir.Create();
    File.WriteAllText(settings.FullName, "{ }");

    // Clean up the entire tree.
    configDir.Delete();
}`}
  language="csharp"
  filename="directory_compose.shard"
/>

<Prose>
  <strong>Conditional creation and safe cleanup.</strong>
</Prose>

<CodeBlock
  code={`using stdio;
using filesystem;

namespace demo;

public static func EnsureWorkDirectory(path: string) -> DirectoryInfo
{
    // Create only when the path is absent.
    if (!Directory.Exists(path))
    {
        return Directory.Create(path);
    }

    return new DirectoryInfo(path);
}

public static func Main() -> void
{
    target: string = "D:/temp/shard_work";
    dir: DirectoryInfo = EnsureWorkDirectory(target);

    // The instance tracks the same path we just ensured.
    println(dir.Exists);       // true
    println(dir.FullName);     // D:/temp/shard_work

    // Safe cleanup: remove only if we actually created it in this run.
    if (dir.Exists)
    {
        dir.Delete();
    }
}`}
  language="csharp"
  filename="directory_conditional.shard"
/>

<Prose>
  <strong>Manual enumeration for planned APIs.</strong> Because{' '}
  <InlineCode>Directory.GetFiles</InlineCode> and{' '}
  <InlineCode>Directory.GetDirectories</InlineCode> are not yet implemented, you can join
  known names to a directory path by hand.
</Prose>

<CodeBlock
  code={`using stdio;
using filesystem;

namespace demo;

public static func Main() -> void
{
    baseDir: DirectoryInfo = Directory.Create("D:/temp/shard_logs");

    // Create a few files so we have something to list.
    File.WriteAllText((baseDir / "app.log").FullName, "app");
    File.WriteAllText((baseDir / "error.log").FullName, "error");

    // Stand-in for Directory.GetFiles: enumerate a known list.
    candidates: string[] = ["app.log", "error.log", "missing.log"];

    foreach (name in candidates)
    {
        file: FileInfo = baseDir / name;

        if (file.Exists)
        {
            println("found: " + file.FullName);
        }
    }

    // Clean up.
    baseDir.Delete();
}`}
  language="csharp"
  filename="directory_manual_enumerate.shard"
/>

<Callout tone="amber" title="Common mistake: treating Exists as creation guarantee">
  <InlineCode>Directory.Exists</InlineCode> and <InlineCode>DirectoryInfo.Exists</InlineCode>
  test the disk at the moment of the call. Between the check and a subsequent operation, the
  directory can be deleted or created by another process. If atomicity matters, call{' '}
  <InlineCode>Directory.Create</InlineCode> directly and handle the resulting exception.
</Callout>

<Callout tone="amber" title="Common mistake: Directory.Delete on a non-empty directory">
  <InlineCode>Directory.Delete(path)</InlineCode> requires the directory to be empty. To
  delete a tree recursively, use <InlineCode>DirectoryInfo.Delete()</InlineCode> on a{' '}
  <InlineCode>DirectoryInfo</InlineCode> instance.
</Callout>

<H2>See also</H2>

<ul className="space-y-2 text-text-secondary">
  <Bullet>
    <InlineCode>File &amp; Path</InlineCode> — reading, writing, and parsing file-system paths.
  </Bullet>
  <Bullet>
    <InlineCode>Path Concatenation</InlineCode> — composing directory and file paths with the <InlineCode>/</InlineCode> operator.
  </Bullet>
</ul>

<H2>Source</H2>

<Prose>
  The <InlineCode>shard.filesystem</InlineCode> implementation ships as part of{" "}
  <InlineCode>ShardScript.Framework</InlineCode>. The native binding for <InlineCode>Directory</InlineCode>,{" "}
  <InlineCode>DirectoryInfo</InlineCode>, and related types is in{" "}
  <InlineCode>ShardScript.Framework/system/filesystem.shard.cpp</InlineCode>.
</Prose>
