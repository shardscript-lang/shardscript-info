---
title: Console I/O
group: SHARD.STDIO
groupOrder: 0
order: 1
slug: console-io
type: Reference
---

<H2>Summary</H2>

<Prose>
  The <InlineCode>shard.stdio</InlineCode> library exposes the <InlineCode>stdio</InlineCode> namespace for
  console input and output. It provides text printing, line reading, cursor control, single-key input, and
  structured object dumping. Every member is <InlineCode>static</InlineCode>; there is no instance of a
  console class.
</Prose>

<H2>Syntax</H2>

<Prose>
  Import the namespace with <InlineCode>using stdio;</InlineCode> and call the helpers directly by name.
  All methods are registered with <InlineCode>LINK_STATIC</InlineCode> on the <InlineCode>stdio</InlineCode>{" "}
  namespace.
</Prose>

<CodeBlock
  code={`using stdio;

namespace demo;

public static func Main() -> void
{
    println("hello");

    name: string = input();
    println("hello, " + name);
}`}
  language="csharp"
  filename="stdio_intro.shard"
/>

<H2>Parameters / Arguments</H2>

<DocsTable
  headers={['Method', 'Parameters', 'Returns', 'Description']}
  rows={[
    [<code>print(message)</code>, <><InlineCode>message</InlineCode>: any printable value</>, 'void', 'Writes the string representation of a value to stdout without a trailing newline.'],
    [<code>println(message)</code>, <><InlineCode>message</InlineCode>: any printable value</>, 'void', 'Writes the string representation of a value followed by a newline.'],
    [<code>println(enumerable)</code>, <><InlineCode>enumerable</InlineCode>: <InlineCode>{'IEnumerable<T>'}</InlineCode></>, 'void', 'Writes the elements of an enumerable between brackets, separated by commas.'],
    [<code>input()</code>, <>(no parameters)</>, 'string', 'Reads a single line from stdin and returns it without the trailing newline.'],
    [<code>input(prompt)</code>, <><InlineCode>prompt</InlineCode>: string</>, 'string', 'Writes the prompt to stdout, then reads and returns a single line from stdin.'],
    [<code>clear()</code>, <>(no parameters)</>, 'void', 'Sends VT100 erase-screen and home-cursor escape codes to the terminal.'],
    [<code>setCursor(x, y)</code>, <><InlineCode>x</InlineCode>: int, <InlineCode>y</InlineCode>: int</>, 'void', 'Moves the cursor to the zero-based column <InlineCode>x</InlineCode> and row <InlineCode>y</InlineCode> using VT100 escape codes.'],
    [<code>setCursorVisible(visible)</code>, <><InlineCode>visible</InlineCode>: bool</>, 'void', 'Shows or hides the text cursor with VT100 escape codes.'],
    [<code>readKey()</code>, <>(no parameters)</>, 'string', 'Reads a single character from stdin without waiting for Enter and returns it as a one-character string.'],
    [<code>Dump(obj)</code>, <><InlineCode>obj</InlineCode>: T</>, 'void', 'Writes a structured, colorized dump of any value to stdout.'],
    [<code>Dump(obj, maxDepth, colorized)</code>, <><InlineCode>obj</InlineCode>: T, <InlineCode>maxDepth</InlineCode>: int, <InlineCode>colorized</InlineCode>: bool</>, 'void', 'Writes a structured dump with configurable recursion depth and color output.'],
  ]}
/>

<H2>Returns</H2>

<DocsTable
  headers={['Member', 'Return Type', 'Notes']}
  rows={[
    [<code>print</code>, 'void', 'No value; output is written directly to native stdout.'],
    [<code>println</code>, 'void', 'No value; output ends with a newline.'],
    [<code>input</code>, 'string', 'The line read from stdin, without the trailing newline. Empty string when stdin is at end-of-file.'],
    [<code>clear</code>, 'void', 'No value; clears the visible terminal buffer.'],
    [<code>setCursor</code>, 'void', 'No value; coordinates are zero-based.'],
    [<code>setCursorVisible</code>, 'void', 'No value; visible cursor may not be supported on every terminal.'],
    [<code>readKey</code>, 'string', 'Single-character string; empty on immediate EOF.'],
    [<code>Dump</code>, 'void', 'No value; structured representation is written to native stdout.'],
  ]}
/>

<H2>Exceptions / Errors</H2>

<ul className="space-y-2 text-text-secondary">
  <Bullet>
    <strong className="text-text-primary">IEnumerable required for enumerable println</strong> —{" "}
    <InlineCode>println(enumerable)</InlineCode> throws when the argument does not implement{" "}
    <InlineCode>{'IEnumerable<T>'}</InlineCode>.
  </Bullet>
  <Bullet>
    <strong className="text-text-primary">Enumerator contract failure</strong> —{" "}
    <InlineCode>println(enumerable)</InlineCode> throws when the enumerator returned by{" "}
    <InlineCode>GetEnumerator</InlineCode> lacks <InlineCode>MoveNext</InlineCode> or{" "}
    <InlineCode>Current</InlineCode>.
  </Bullet>
  <Bullet>
    <strong className="text-text-primary">VT100 support required</strong> —{" "}
    <InlineCode>clear</InlineCode>, <InlineCode>setCursor</InlineCode>, and{" "}
    <InlineCode>setCursorVisible</InlineCode> emit ANSI escape sequences. Terminals that do not interpret
    VT100 codes display raw escape characters instead of changing the cursor or screen.
  </Bullet>
</ul>

<H2>Remarks</H2>

<Prose>
  <strong>Static namespace methods.</strong> Every helper is attached directly to the{" "}
  <InlineCode>stdio</InlineCode> namespace with <InlineCode>LINK_STATIC</InlineCode>. There is no console
  object to construct; calls compile to <InlineCode>CALLSTATICMETHODSYMBOL</InlineCode> and resolve at
  compile time.
</Prose>

<Prose>
  <strong>Output channel.</strong> The stdio helpers write through native{" "}
  <InlineCode>std::wcout</InlineCode>. On Windows the library attempts to enable VT100 processing during
  startup so that color and cursor escape codes work in modern consoles. Output is not returned as a
  ShardScript string and cannot be captured by assigning the result of <InlineCode>println</InlineCode>.
</Prose>

<Prose>
  <strong>Prompt input.</strong> The <InlineCode>input(prompt: string)</InlineCode> overload writes the
  prompt to <InlineCode>std::wcout</InlineCode> without a trailing newline, then reads a line from{" "}
  <InlineCode>std::wcin</InlineCode>. It is equivalent to calling <InlineCode>print(prompt)</InlineCode>{" "}
  followed by <InlineCode>input()</InlineCode>, but avoids the separate statement.
</Prose>

<Prose>
  <strong>Dump output format.</strong> <InlineCode>Dump</InlineCode> renders primitives, arrays, enums, and
  objects. Null values print as <InlineCode>null</InlineCode>, booleans as <InlineCode>true</InlineCode> or
  <InlineCode>false</InlineCode>, integers/doubles as numbers, chars inside single quotes, and strings
  inside double quotes with escaped control characters. Arrays render as comma-separated lists inside
  square brackets. Objects render as indented <InlineCode>{'{ key: value }'}</InlineCode> blocks that include
  public properties first, then fields. Compiler-generated backing fields are omitted.
</Prose>

<Prose>
  <strong>Depth and circularity protection.</strong> The default dump stops at a depth of 3. Deeper objects
  are summarized as <InlineCode>[object TypeName]</InlineCode>. If the object graph contains a cycle, the
  dumper prints <InlineCode>[Circular]</InlineCode> instead of recursing into the repeated object. These
  guards prevent infinite output and stack overflow when dumping arbitrary object graphs.
</Prose>

<Prose>
  <strong>Colorization.</strong> The default <InlineCode>Dump(value)</InlineCode> overload uses ANSI colors:
  magenta for null and booleans, green for strings and characters, yellow for numbers, cyan for object
  keys, and gray for type annotations and truncation markers. Pass <InlineCode>false</InlineCode> as the
  third argument to disable colors, or call the overload with explicit <InlineCode>maxDepth</InlineCode> and
  <InlineCode>colorized</InlineCode> parameters.
</Prose>

<Prose>
  <strong>Properties take precedence over fields.</strong> When dumping an object, the dumper first
  iterates declared properties with public getters, then iterates fields that have not already been printed
  as a property. This avoids duplicate output for auto-implemented properties that store their value in a
  compiler-generated backing field.
</Prose>

<H2>Examples</H2>

<Prose>
  <strong>Basic printing and reading.</strong>
</Prose>

<CodeBlock
  code={`using stdio;

namespace demo;

public static func Main() -> void
{
    print("Enter your name: ");
    name: string = input();
    println("Hello, " + name + "!");
}`}
  language="csharp"
  filename="stdio_basic.shard"
/>

<Prose>
  <strong>Reading with a prompt.</strong>
</Prose>

<CodeBlock
  code={`using stdio;

namespace demo;

public static func Main() -> void
{
    // The prompt is written to stdout before reading the line.
    name: string = input("Enter your name: ");
    println("Hello, " + name + "!");
}`}
  language="csharp"
  filename="stdio_input_prompt.shard"
/>

<Prose>
  <strong>Dumping primitives and objects.</strong>
</Prose>

<CodeBlock
  code={`using stdio;

namespace demo;

public class Point
{
    public X: int;
    public Y: int;

    public init(x: int, y: int)
    {
        this.X = x;
        this.Y = y;
    }
}

public class Person
{
    public Name: string;
    public Age: int;
    public Location: Point;

    public init(name: string, age: int)
    {
        this.Name = name;
        this.Age = age;
        this.Location = new Point(10, 20);
    }
}

public static func Main() -> void
{
    person: Person = new Person("Alice", 30);

    println("=== default Dump ===");
    Dump(person);

    println("=== Dump with depth=1, no colors ===");
    Dump(person, 1, false);

    println("=== Dump primitives ===");
    Dump(null);
    Dump(true);
    Dump(42);
    Dump(3.14);
    Dump('A');
    Dump("hello world");
}`}
  language="csharp"
  filename="stdio_dump.shard"
/>

<Prose>
  <strong>Dumping arrays and limiting recursion.</strong>
</Prose>

<CodeBlock
  code={`using stdio;

namespace demo;

public static func Main() -> void
{
    values: int[] = [1, 2, 3];
    Dump(values);

    // Limit how deeply nested objects are expanded.
    nested: int[][] = [[1, 2], [3, 4]];
    Dump(nested, 1, false);
}`}
  language="csharp"
  filename="stdio_dump_array.shard"
/>

<Prose>
  <strong>Terminal cursor and screen control.</strong>
</Prose>

<CodeBlock
  code={`using stdio;

namespace demo;

public static func Main() -> void
{
    // Hide the cursor while drawing.
    setCursorVisible(false);

    // Clear the screen and move to the top-left cell.
    clear();
    setCursor(0, 0);
    println("drawn at the top-left corner");

    // Show the cursor again before exiting.
    setCursorVisible(true);
}`}
  language="csharp"
  filename="stdio_cursor.shard"
/>

<Prose>
  <strong>Reading a single key press.</strong>
</Prose>

<CodeBlock
  code={`using stdio;

namespace demo;

public static func Main() -> void
{
    println("Press any key...");
    key: string = readKey();
    println("You pressed: " + key);
}`}
  language="csharp"
  filename="stdio_readkey.shard"
/>

<Callout tone="blue">
  Use <InlineCode>Dump</InlineCode> for debugging and interactive inspection only. It writes directly to
  native stdout, performs reflection over every field and property, and can produce large output for deep
  object graphs. Do not leave <InlineCode>Dump</InlineCode> calls in production hot paths.
</Callout>

<H2>See also</H2>

<ul className="space-y-2 text-text-secondary">
  <Bullet>
    <InlineCode>Developer Tools</InlineCode> — runtime introspection helpers such as{" "}
    <InlineCode>typeof</InlineCode> and <InlineCode>sizeof</InlineCode>.
  </Bullet>
  <Bullet>
    <InlineCode>Console I/O</InlineCode> — this article; the <InlineCode>stdio</InlineCode> namespace reference.
  </Bullet>
</ul>

<H2>Source</H2>

<Prose>
  The console I/O implementation ships as part of <InlineCode>ShardScript.Framework</InlineCode>. The native
  binding for <InlineCode>print</InlineCode>, <InlineCode>println</InlineCode>, <InlineCode>input</InlineCode>,
  <InlineCode>readKey</InlineCode>, <InlineCode>Dump</InlineCode>, and related helpers is in{" "}
  <InlineCode>ShardScript.Framework/system/constream.shard.cpp</InlineCode>.
</Prose>
