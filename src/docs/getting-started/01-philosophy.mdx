---
title: ShardScript Philosophy
group: Getting Started
order: 1
slug: shardscript-philosophy
groupOrder: 1
---

<H2>Summary</H2>

<Prose>
  ShardScript is an <strong>embeddable, compiled scripting language with strict static typing</strong>,
  implemented in C++20. It is designed to occupy the middle ground between lightweight dynamically-typed
  embeddable languages and heavyweight statically-typed application languages: it offers the ease of
  embedding and rapid iteration of a script engine while catching type, name, and contract errors at
  compile time.
</Prose>

<H2>What problem it solves</H2>

<Prose>
  Applications that need user-facing scripting usually choose between two extremes. On one side are
  dynamically-typed languages such as Lua or Python: they are easy to embed, have rich ecosystems, and
  allow writers to iterate quickly. On the other side are statically-typed languages such as C# or full
  C++ plugins: they catch errors early and scale well, but they are heavier to host, slower to compile,
  and harder to sandbox.
</Prose>

<Prose>
  ShardScript targets the gap between those extremes. It is meant for hosts that want:
</Prose>

<ul className="space-y-2 text-text-secondary">
  <Bullet>
    <strong className="text-text-primary">Compile-time confidence in script code</strong> — typos, missing
    members, and type mismatches are reported before the script ever runs.
  </Bullet>
  <Bullet>
    <strong className="text-text-primary">A small runtime footprint</strong> — a custom stack-based virtual
    machine and a compact bytecode format, not a full language runtime or JIT compiler.
  </Bullet>
  <Bullet>
    <strong className="text-text-primary">Direct C++ interoperability</strong> — native libraries are
    ordinary shared libraries (DLL / SO / dylib) that export two well-known entry points, so hosts and
    third-party authors can extend the language without modifying the compiler.
  </Bullet>
  <Bullet>
    <strong className="text-text-primary">Familiar syntax</strong> — a C-style grammar with classes,
    generics, delegates, lambdas, and async/await, lowering the barrier for developers who already know
    C#, C++, Java, or TypeScript.
  </Bullet>
</ul>

<H2>Key ideas</H2>

<Prose>
  <strong>Compiled, not interpreted.</strong> ShardScript pays the compilation cost once, ahead of time,
  rather than repeatedly during execution. The result is faster startup of hot paths, earlier error
  detection, and the option to validate scripts during a build step rather than in production.
</Prose>

<Prose>
  <strong>Strict static typing.</strong> Every variable, parameter, field, and return type has a compile-time
  type. Generics, interfaces, classes, structs, and enums are first-class, and overload resolution happens
  at compile time. The language does not fall back to dynamic dispatch to resolve member access.
</Prose>

<Prose>
  <strong>Embeddable by design.</strong> The runtime is a library, not a standalone process. A host
  initializes a <InlineCode>CompilationContext</InlineCode>, compiles source into an{" "}
  <InlineCode>ApplicationDomain</InlineCode>, and calls <InlineCode>VM::Run()</InlineCode> on the entry
  point. Native libraries are loaded as shared objects at runtime and register their symbols through the
  same API the standard library uses.
</Prose>

<Prose>
  <strong>Pragmatic, not purist.</strong> ShardScript allows multiple ways to solve common problems when
  each way carries a real ergonomic benefit. <InlineCode>defer</InlineCode> gives deterministic cleanup at
  scope exit, while <InlineCode>IDisposable</InlineCode> lets authors attach custom cleanup logic to the
  same mechanism. Properties, indexers, operator overloading, and extension methods are all available when
  they make an API clearer.
</Prose>

<Callout tone="blue" title="Example">
  <InlineCode>defer</InlineCode> releases resources on scope exit, while <InlineCode>IDisposable</InlineCode>{' '}
  lets you hook custom cleanup logic into that same mechanism.
</Callout>

<Prose>
  <strong>First-class native interop.</strong> Script code can call into C++ through native libraries, and
  native code can invoke script methods, read fields, and allocate script objects. This makes ShardScript
  suitable as an extension language for C++ applications where tight integration with existing C++ data
  structures and algorithms matters.
</Prose>

<Prose>
  <strong>Cooperative concurrency.</strong> Asynchronous methods compile into state machines that yield
  control back to the event loop on <InlineCode>await</InlineCode>. There are no OS threads created by the
  language; concurrency is single-threaded and cooperative, simplifying memory safety and avoiding data
  races in script code.
</Prose>

<H2>When to use / When not to use</H2>

<DocsTable
  headers={['Use ShardScript when', 'Consider alternatives when']}
  rows={[
    [
      <>You need a script engine inside a C++ application and want compile-time safety for user-written code.</>,
      <>You need a read-eval-print loop (REPL), rapid runtime introspection, or duck typing. ShardScript is compiled and statically typed.</>,
    ],
    [
      <>You want to ship pre-validated scripts and catch errors during a build or mod-publishing step.</>,
      <>You need a mature package ecosystem, IDE tooling, or language-specific hiring market. ShardScript is a younger, focused language.</>,
    ],
    [
      <>Your project benefits from C-style syntax, classes, generics, async/await, and native library extensions.</>,
      <>You need direct memory layout control, manual memory management, or zero-overhead abstractions. Use C or C++ directly.</>,
    ],
    [
      <>You want deterministic cleanup patterns such as <InlineCode>defer</InlineCode> and <InlineCode>IDisposable</InlineCode> alongside automatic garbage collection.</>,
      <>You need multi-threaded parallelism in script code. ShardScript concurrency is cooperative and single-threaded.</>,
    ],
  ]}
/>

<H2>Related articles</H2>

<ul className="space-y-2 text-text-secondary">
  <Bullet>
    <InlineCode>Runtime Architecture</InlineCode> — how source becomes bytecode and how the VM executes it.
  </Bullet>
  <Bullet>
    <InlineCode>Installation &amp; Setup</InlineCode> — building ShardScript and running your first program.
  </Bullet>
  <Bullet>
    <InlineCode>Native Library Overview</InlineCode> — extending ShardScript with C++ shared libraries.
  </Bullet>
  <Bullet>
    <InlineCode>Classes</InlineCode> — object-oriented features in ShardScript.
  </Bullet>
  <Bullet>
    <InlineCode>Functions and Delegates</InlineCode> — functional programming constructs.
  </Bullet>
</ul>
