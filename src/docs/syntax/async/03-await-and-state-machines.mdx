---
title: Await and State Machines
group: Asynchronous Programming
order: 3
slug: await-and-state-machines
type: Concept
groupOrder: 6
---

<Prose>
  The <InlineCode>await</InlineCode> keyword is the suspension point in an <InlineCode>async</InlineCode>{' '}
  method. When execution reaches <InlineCode>await</InlineCode>, the method returns control to the
  event loop, registers a continuation, and later resumes exactly where it left off. The compiler
  makes this possible by rewriting every <InlineCode>async</InlineCode> method into a state machine
  whose <InlineCode>MoveNext</InlineCode> method is driven by an integer <InlineCode>State</InlineCode>{' '}
  field.
</Prose>

<H2>Introduction</H2>

<Prose>
  ShardScript runs asynchronous work on a single-threaded libuv event loop. An <InlineCode>async</InlineCode>{' '}
  method never blocks an OS thread while it waits for a timer, file, socket, or other background
  operation. Instead, it <em>suspends</em> at an <InlineCode>await</InlineCode> expression, stores its
  local state, and lets the loop process other work. When the awaited operation completes, the
  runtime calls the stored continuation, which re-enters the method and continues from the next
  statement.
</Prose>

<Prose>
  This behavior is implemented by the compiler, not by the runtime alone. Before bytecode is emitted,
  the <InlineCode>AsyncStateMachineLowering</InlineCode> pass transforms each eligible async method
  into a compiler-generated class. The original method body becomes a <InlineCode>MoveNext</InlineCode>{' '}
  method that switches on the current state, and every local variable that must survive a suspension
  is lifted into a field.
</Prose>

<H2>What it is</H2>

<Prose>
  <strong>Await is a unary operator.</strong> It takes an awaitable expression on its right and
  either returns the awaitable's result immediately (if it is already completed) or suspends the
  current async method until the awaitable completes. The operand must implement the{' '}
  <InlineCode>async.IAwaitable</InlineCode> interface, which exposes a <InlineCode>GetAwaiter</InlineCode>{' '}
  method. The awaiter must implement <InlineCode>async.IAwaiter</InlineCode> with{' '}
  <InlineCode>IsCompleted</InlineCode>, <InlineCode>OnCompleted</InlineCode>, and{' '}
  <InlineCode>GetResult</InlineCode> members.
</Prose>

<DocsTable
  headers={['Member', 'Purpose']}
  rows={[
    [<code>IAwaitable.GetAwaiter()</code>, 'Returns an IAwaiter that tracks completion and produces the result.'],
    [<code>IAwaiter.IsCompleted</code>, 'True if the operation has already finished.'],
    [<code>IAwaiter.OnCompleted(IAsyncState)</code>, 'Registers the state machine so it is resumed when the operation completes.'],
    [<code>IAwaiter.GetResult()</code>, 'Returns the result of the completed operation.'],
  ]}
/>

<Prose>
  <strong>The state machine is the continuation.</strong> When the compiler lowers an async method,
  it generates a private class that implements <InlineCode>IAsyncState</InlineCode>. That interface
  has a single member, <InlineCode>MoveNext()</InlineCode>, which the runtime invokes each time the
  method resumes. The generated class also stores:
</Prose>

<ul className="space-y-2 text-text-secondary">
  <Bullet>
    A <InlineCode>State</InlineCode> integer that records which suspension point to continue from.
  </Bullet>
  <Bullet>
    The <InlineCode>Task</InlineCode> or <InlineCode>ValueTask</InlineCode> object that represents the
    async method's own completion.
  </Bullet>
  <Bullet>
    Lifted copies of every parameter and local variable that are live across an{' '}
    <InlineCode>await</InlineCode>.
  </Bullet>
  <Bullet>
    An optional <InlineCode>OuterThis</InlineCode> reference when the async method is an instance
    member.
  </Bullet>
</ul>

<Prose>
  <strong>Lowering is a three-pass pipeline.</strong> <InlineCode>AsyncStateMachineLowering</InlineCode>{' '}
  runs after semantic analysis and before layout:
</Prose>

<ol className="space-y-2 text-text-secondary">
  <Bullet>
    <strong>Hoisting pass.</strong> Nested awaits inside expressions, conditions, and return
    statements are rewritten as top-level statements with temporary variables. This keeps the
    subsequent passes simple and predictable.
  </Bullet>
  <Bullet>
    <strong>Analysis pass.</strong> The rewritten body is scanned for await sites. For each site, the
    pass records the active <InlineCode>try</InlineCode> regions, the statement that follows the
    await, and any variable that receives the await result.
  </Bullet>
  <Bullet>
    <strong>Emission pass.</strong> After layout assigns field and slot offsets, bytecode is emitted
    for the state-machine constructor, the factory method that creates it, and the{' '}
    <InlineCode>MoveNext</InlineCode> body.
  </Bullet>
</ol>

<H2>When to use it</H2>

<Prose>
  Use <InlineCode>await</InlineCode> whenever an async method must wait for an operation without
  blocking the event loop. Typical cases include timers, I/O, network calls, thread-pool offloads, and
  composing multiple async operations.
</Prose>

<ul className="space-y-2 text-text-secondary">
  <Bullet>
    <strong>Timers and delays.</strong> <InlineCode>await Task.Delay(milliseconds)</InlineCode> yields
    cooperatively and resumes after the interval.
  </Bullet>
  <Bullet>
    <strong>I/O and sockets.</strong> File and socket operations register libuv handles; the method
    suspends until data is ready.
  </Bullet>
  <Bullet>
    <strong>Composing async helpers.</strong> Await another <InlineCode>async</InlineCode> method to
    reuse sequential asynchronous logic.
  </Bullet>
  <Bullet>
    <strong>Consuming results.</strong> Await a <InlineCode>ValueTask&lt;T&gt;</InlineCode> to obtain
    the typed result when it becomes available.
  </Bullet>
</ul>

<Callout tone="blue">
  Await is only valid inside an <InlineCode>async</InlineCode> method. The compiler rejects{' '}
  <InlineCode>await</InlineCode> in synchronous methods such as <InlineCode>Main</InlineCode>. To block
  synchronously on an async operation, call <InlineCode>Task.Wait(task)</InlineCode> or{' '}
  <InlineCode>ValueTask.Wait(task)</InlineCode>.
</Callout>

<H2>Remarks</H2>

<Prose>
  <strong>Suspension is cooperative and single-threaded.</strong> When <InlineCode>await</InlineCode>{' '}
  is reached, the runtime checks <InlineCode>IsCompleted</InlineCode>. If the operation is already
  done, execution continues synchronously. Otherwise the runtime stores the state machine as the
  continuation on the awaiter, sets the state machine's <InlineCode>State</InlineCode> to the resume
  label, and returns from <InlineCode>MoveNext</InlineCode>. The libuv loop then processes other
  timers and I/O until the operation completes and calls <InlineCode>OnCompleted</InlineCode>.
</Prose>

<Prose>
  <strong>Resumption reads the continuation field.</strong> The runtime helper{' '}
  <InlineCode>ResumeContinuation</InlineCode> retrieves the stored <InlineCode>IAsyncState</InlineCode>,
  calls its <InlineCode>MoveNext</InlineCode> method, and the generated switch jumps to the correct
  resume label. The task is rooted by the event loop while suspended so the garbage collector cannot
  reclaim it.
</Prose>

<Prose>
  <strong>Locals survive suspension as fields.</strong> Any parameter or local variable whose value is
  needed after an <InlineCode>await</InlineCode> is copied into a field on the state machine before
  the method returns, and copied back into a local slot when <InlineCode>MoveNext</InlineCode>{' '}
  resumes. Variables that are no longer live after the await are not lifted, keeping the state machine
  small.
</Prose>

<Prose>
  <strong>Exception handling is preserved across awaits.</strong> If an <InlineCode>await</InlineCode>{' '}
  appears inside a <InlineCode>try</InlineCode> block, the analysis pass records the active try stack at
  the resume point. When the method resumes, the emitter restores the correct exception-handler
  context before executing the code that follows the await. If the awaited operation faults, the
  exception is propagated through the stored continuation and dispatched to the matching{' '}
  <InlineCode>catch</InlineCode> clause.
</Prose>

<Prose>
  <strong>Loops and conditionals require enumerator hoisting.</strong> A <InlineCode>foreach</InlineCode>{' '}
  or <InlineCode>for-in</InlineCode> loop that contains an <InlineCode>await</InlineCode> must store its
  enumerator in a state-machine field, because the enumerator object must survive across suspensions.
  The same applies to the loop variable and any condition expression that is evaluated after each
  iteration.
</Prose>

<Callout tone="amber" title="Expression-level awaits">
  The compiler supports awaits inside expressions such as return values, conditions, and assignments,
  but it first rewrites them into top-level statements during the hoisting pass. Write awaits as
  separate statements when possible; it makes the generated state machine smaller and easier to
  diagnose if lowering fails.
</Callout>

<Callout tone="amber" title="Async lambdas">
  The language supports <InlineCode>async</InlineCode> methods on classes and top-level functions.
  Async lambda expressions are recognized by the parser in some contexts, but full state-machine
  lowering for lambdas is still being stabilized. Prefer named async methods for production code until
  support is complete.
</Callout>

<H2>Examples</H2>

<Prose>
  <strong>Basic suspension and resume.</strong> The simplest await yields the loop and resumes after a
  timer fires.
</Prose>

<CodeBlock
  code={`using stdio;
using async;

namespace demo;

public class Program
{
    public static async func DelayAndReport() -> Task
    {
        println("before await");
        await Task.Delay(100);
        println("after await");
    }

    public static func Main() -> void
    {
        Task task = DelayAndReport();
        Task.Wait(task);
    }
}`}
  language="csharp"
  filename="basic_await.shard"
/>

<Prose>
  <strong>Awaiting a result with ValueTask.</strong> Await returns the underlying value once the
  operation completes.
</Prose>

<CodeBlock
  code={`using stdio;
using async;

namespace demo;

public class Program
{
    public static async func ComputeAsync() -> ValueTask<int>
    {
        await Task.Delay(10);
        return 42;
    }

    public static async func UseResultAsync() -> Task
    {
        int value = await ComputeAsync();
        println(value + 1);
    }

    public static func Main() -> void
    {
        Task task = UseResultAsync();
        Task.Wait(task);
    }
}`}
  language="csharp"
  filename="valuetask_await.shard"
/>

<Prose>
  <strong>Await inside a loop.</strong> Each iteration suspends independently. The loop variable and
  any condition state are preserved across suspensions.
</Prose>

<CodeBlock
  code={`using stdio;
using async;

namespace demo;

public class Program
{
    public static async func CountWithPauses() -> Task
    {
        int i = 0;
        while (i < 3)
        {
            println(i);
            await Task.Delay(10);
            i = i + 1;
        }

        println("done");
    }

    public static func Main() -> void
    {
        Task task = CountWithPauses();
        Task.Wait(task);
    }
}`}
  language="csharp"
  filename="await_in_loop.shard"
/>

<Prose>
  <strong>Await inside conditionals.</strong> Both branches of an <InlineCode>if/else</InlineCode> can
  contain awaits. The state machine records which branch was taken so it resumes at the correct
  statement.
</Prose>

<CodeBlock
  code={`using stdio;
using async;

namespace demo;

public class Program
{
    public static async func ToggleAsync() -> Task
    {
        int i = 0;
        while (i < 4)
        {
            if (i % 2 == 0)
            {
                println("even before");
                await Task.Delay(1);
                println("even after");
            }
            else
            {
                println("odd before");
                await Task.Delay(1);
                println("odd after");
            }

            i = i + 1;
        }

        println("done");
    }

    public static func Main() -> void
    {
        Task task = ToggleAsync();
        Task.Wait(task);
    }
}`}
  language="csharp"
  filename="await_in_conditional.shard"
/>

<Prose>
  <strong>Nested awaits.</strong> Awaiting an async method that itself awaits another operation works
  because each method has its own state machine. The outer continuation is registered on the inner
  task, and the inner continuation is registered on the timer or I/O handle.
</Prose>

<CodeBlock
  code={`using stdio;
using async;

namespace demo;

public class Program
{
    public static async func InnerAsync() -> ValueTask<int>
    {
        await Task.Delay(10);
        return 7;
    }

    public static async func OuterAsync() -> ValueTask<int>
    {
        await Task.Delay(10);
        int inner = await InnerAsync();
        return inner * 2;
    }

    public static func Main() -> void
    {
        ValueTask<int> task = OuterAsync();
        ValueTask.Wait(task);
        println(task.Result);
    }
}`}
  language="csharp"
  filename="nested_await.shard"
/>

<Prose>
  <strong>Custom awaitable.</strong> You can implement <InlineCode>IAwaitable</InlineCode> and{' '}
  <InlineCode>IAwaiter</InlineCode> to make any type awaitable. The example below completes
  synchronously, so <InlineCode>IsCompleted</InlineCode> returns <InlineCode>true</InlineCode> and no
  continuation is scheduled.
</Prose>

<CodeBlock
  code={`using stdio;
using async;

namespace demo;

public class ImmediateAwaiter : IAwaiter
{
    private Result: int;

    public init(value: int)
    {
        this.Result = value;
    }

    public func IsCompleted() -> bool
    {
        return true;
    }

    public func OnCompleted(continuation: IAsyncState) -> void
    {
        // Already completed, so no continuation is needed.
    }

    public func GetResult() -> int
    {
        return this.Result;
    }
}

public class ImmediateAwaitable : IAwaitable
{
    private Value: int;

    public init(value: int)
    {
        this.Value = value;
    }

    public func GetAwaiter() -> ImmediateAwaiter
    {
        return new ImmediateAwaiter(this.Value);
    }
}

public class Program
{
    public static async func TestCustomAwaitable() -> Task
    {
        ImmediateAwaitable awaitable = new ImmediateAwaitable(123);
        int result = await awaitable;
        println(result);
    }

    public static func Main() -> void
    {
        Task task = TestCustomAwaitable();
        Task.Wait(task);
    }
}`}
  language="csharp"
  filename="custom_awaitable.shard"
/>

<Prose>
  <strong>Exception handling across await.</strong> Exceptions thrown by an awaited operation are
  delivered to the active <InlineCode>try</InlineCode> block in the awaiting method.
</Prose>

<CodeBlock
  code={`using stdio;
using async;

namespace demo;

public class Program
{
    public static async func FaultyDelay() -> Task
    {
        await Task.Delay(10);
        throw new RuntimeException();
    }

    public static async func CaughtAwait() -> Task
    {
        try
        {
            await FaultyDelay();
            println("unreachable");
        }
        catch (ex: RuntimeException)
        {
            println("caught inside async");
            await Task.Delay(10);
            println(ex.message);
        }

        println("after catch");
    }

    public static func Main() -> void
    {
        Task task = CaughtAwait();
        Task.Wait(task);
    }
}`}
  language="csharp"
  filename="await_exception.shard"
/>

<Prose>
  <strong>Common mistakes.</strong> These patterns are rejected by the compiler or produce surprising
  behavior.
</Prose>

<CodeBlock
  code={`using stdio;
using async;

namespace demo;

public class Program
{
    public static async func FireAndForget() -> Task
    {
        await Task.Delay(10);
        println("fires later");
    }

    public static func Main() -> void
    {
        // This starts the async method but does not wait for it.
        // The program may exit before "fires later" is printed.
        FireAndForget();

        // Correct: capture the task and wait.
        Task task = FireAndForget();
        Task.Wait(task);
    }
}`}
  language="csharp"
  filename="await_mistakes.shard"
/>

<Callout tone="red" title="Await outside async">
  Writing <InlineCode>await Task.Delay(100);</InlineCode> inside a synchronous method such as{' '}
  <InlineCode>Main</InlineCode> produces a compile error. Use <InlineCode>Task.Wait(task)</InlineCode>{' '}
  to bridge sync and async code.
</Callout>

<Callout tone="red" title="Forgetting to wait">
  Calling an async method without awaiting or waiting on the returned task starts it concurrently but
  gives no guarantee it completes before the program exits. Always wait on the returned task unless
  the runtime explicitly keeps it alive.
</Callout>
